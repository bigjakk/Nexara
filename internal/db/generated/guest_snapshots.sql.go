// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: guest_snapshots.sql

package db

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const deleteGuestSnapshotsForVanishedGuests = `-- name: DeleteGuestSnapshotsForVanishedGuests :execrows
DELETE FROM guest_snapshots
WHERE cluster_id = $1
  AND NOT (vmid = ANY($2::int[]))
`

type DeleteGuestSnapshotsForVanishedGuestsParams struct {
	ClusterID uuid.UUID `json:"cluster_id"`
	Vmids     []int32   `json:"vmids"`
}

// DeleteGuestSnapshotsForVanishedGuests removes snapshot rows for guests that
// no longer exist in the cluster's vms inventory. Callers feed vmids from the
// vms table (not from a live listing), so this inherits the grace protection
// DeleteStaleVMsForNodes gives vms rows — a transient per-node Proxmox blip
// cannot cascade into snapshot-inventory loss. The vmid list must be a
// non-nil (possibly empty) slice: pgx encodes nil as SQL NULL and the DELETE
// then silently matches nothing.
func (q *Queries) DeleteGuestSnapshotsForVanishedGuests(ctx context.Context, arg DeleteGuestSnapshotsForVanishedGuestsParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteGuestSnapshotsForVanishedGuests, arg.ClusterID, arg.Vmids)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteGuestSnapshotsNotInSet = `-- name: DeleteGuestSnapshotsNotInSet :execrows
DELETE FROM guest_snapshots
WHERE cluster_id = $1
  AND vmid = $2
  AND NOT (name = ANY($3::text[]))
`

type DeleteGuestSnapshotsNotInSetParams struct {
	ClusterID uuid.UUID `json:"cluster_id"`
	Vmid      int32     `json:"vmid"`
	Names     []string  `json:"names"`
}

// DeleteGuestSnapshotsNotInSet removes a single guest's snapshots that are
// absent from a freshly listed set. The snapshot listing endpoint is
// config-authoritative (it reads the guest's config file), so no grace window
// is needed — but callers must only invoke this for guests whose listing
// succeeded this pass, and must treat a raw-empty listing as anomalous (PVE
// always returns at least the synthetic "current" entry). An empty name list
// is valid: a guest whose real snapshots were all deleted prunes everything —
// but it must be a non-nil empty slice. pgx encodes a nil slice as SQL NULL,
// and NOT (x = ANY(NULL)) is NULL, so a nil list silently deletes nothing.
func (q *Queries) DeleteGuestSnapshotsNotInSet(ctx context.Context, arg DeleteGuestSnapshotsNotInSetParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteGuestSnapshotsNotInSet, arg.ClusterID, arg.Vmid, arg.Names)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getClusterSnapshotAgeStats = `-- name: GetClusterSnapshotAgeStats :one
SELECT
    o.name AS oldest_name,
    o.vmid AS oldest_vmid,
    ((extract(epoch FROM now()) - o.snap_time) / 86400.0)::float8 AS oldest_age_days,
    (SELECT count(*)
       FROM guest_snapshots gs
      WHERE gs.cluster_id = $1
        AND gs.snap_time > 0
        AND (extract(epoch FROM now()) - gs.snap_time) / 86400.0 > $2::float8
    ) AS over_count
FROM guest_snapshots o
WHERE o.cluster_id = $1
  AND o.snap_time > 0
ORDER BY o.snap_time ASC
LIMIT 1
`

type GetClusterSnapshotAgeStatsParams struct {
	ClusterID     uuid.UUID `json:"cluster_id"`
	ThresholdDays float64   `json:"threshold_days"`
}

type GetClusterSnapshotAgeStatsRow struct {
	OldestName    string  `json:"oldest_name"`
	OldestVmid    int32   `json:"oldest_vmid"`
	OldestAgeDays float64 `json:"oldest_age_days"`
	OverCount     int64   `json:"over_count"`
}

// GetClusterSnapshotAgeStats backs the snapshot_age_days alert metric for
// cluster-scoped rules: the oldest dated snapshot in the cluster plus how many
// exceed the rule threshold. Rows with snap_time = 0 (age unknown) are
// excluded — computing an age from 0 would read as ~56 years and false-fire.
// Zero dated snapshots → no row (ErrNoRows), which callers treat as
// condition-not-met so the alert auto-resolves after cleanup.
func (q *Queries) GetClusterSnapshotAgeStats(ctx context.Context, arg GetClusterSnapshotAgeStatsParams) (GetClusterSnapshotAgeStatsRow, error) {
	row := q.db.QueryRow(ctx, getClusterSnapshotAgeStats, arg.ClusterID, arg.ThresholdDays)
	var i GetClusterSnapshotAgeStatsRow
	err := row.Scan(
		&i.OldestName,
		&i.OldestVmid,
		&i.OldestAgeDays,
		&i.OverCount,
	)
	return i, err
}

const getVMSnapshotAgeStats = `-- name: GetVMSnapshotAgeStats :one
SELECT
    o.name AS oldest_name,
    o.vmid AS oldest_vmid,
    ((extract(epoch FROM now()) - o.snap_time) / 86400.0)::float8 AS oldest_age_days,
    (SELECT count(*)
       FROM guest_snapshots gs
      WHERE gs.cluster_id = $1
        AND gs.vmid = $2
        AND gs.snap_time > 0
        AND (extract(epoch FROM now()) - gs.snap_time) / 86400.0 > $3::float8
    ) AS over_count
FROM guest_snapshots o
WHERE o.cluster_id = $1
  AND o.vmid = $2
  AND o.snap_time > 0
ORDER BY o.snap_time ASC
LIMIT 1
`

type GetVMSnapshotAgeStatsParams struct {
	ClusterID     uuid.UUID `json:"cluster_id"`
	Vmid          int32     `json:"vmid"`
	ThresholdDays float64   `json:"threshold_days"`
}

type GetVMSnapshotAgeStatsRow struct {
	OldestName    string  `json:"oldest_name"`
	OldestVmid    int32   `json:"oldest_vmid"`
	OldestAgeDays float64 `json:"oldest_age_days"`
	OverCount     int64   `json:"over_count"`
}

// GetVMSnapshotAgeStats is the vm-scoped counterpart of
// GetClusterSnapshotAgeStats; same snap_time > 0 and ErrNoRows semantics.
func (q *Queries) GetVMSnapshotAgeStats(ctx context.Context, arg GetVMSnapshotAgeStatsParams) (GetVMSnapshotAgeStatsRow, error) {
	row := q.db.QueryRow(ctx, getVMSnapshotAgeStats, arg.ClusterID, arg.Vmid, arg.ThresholdDays)
	var i GetVMSnapshotAgeStatsRow
	err := row.Scan(
		&i.OldestName,
		&i.OldestVmid,
		&i.OldestAgeDays,
		&i.OverCount,
	)
	return i, err
}

const listAllGuestSnapshots = `-- name: ListAllGuestSnapshots :many
SELECT
    gs.cluster_id,
    gs.vmid,
    gs.name,
    gs.guest_type,
    gs.node,
    gs.description,
    gs.parent,
    gs.vmstate,
    gs.snap_time,
    gs.last_seen_at,
    c.name   AS cluster_name,
    v.id     AS vm_id,
    v.name   AS vm_name,
    v.status AS vm_status
FROM guest_snapshots gs
JOIN clusters c ON c.id = gs.cluster_id
LEFT JOIN vms v ON v.cluster_id = gs.cluster_id AND v.vmid = gs.vmid
ORDER BY (gs.snap_time = 0), gs.snap_time ASC, gs.cluster_id, gs.vmid, gs.name
`

type ListAllGuestSnapshotsRow struct {
	ClusterID   uuid.UUID   `json:"cluster_id"`
	Vmid        int32       `json:"vmid"`
	Name        string      `json:"name"`
	GuestType   string      `json:"guest_type"`
	Node        string      `json:"node"`
	Description string      `json:"description"`
	Parent      string      `json:"parent"`
	Vmstate     bool        `json:"vmstate"`
	SnapTime    int64       `json:"snap_time"`
	LastSeenAt  time.Time   `json:"last_seen_at"`
	ClusterName string      `json:"cluster_name"`
	VmID        pgtype.UUID `json:"vm_id"`
	VmName      pgtype.Text `json:"vm_name"`
	VmStatus    pgtype.Text `json:"vm_status"`
}

// ListAllGuestSnapshots feeds the central snapshots page. vms is LEFT JOINed
// on the stable (cluster_id, vmid) identity so rows whose guest row is mid-
// churn (or gone) still render; vm_id/vm_name/vm_status are NULL then and the
// frontend disables the guest link. Unknown ages (snap_time = 0) sort last.
func (q *Queries) ListAllGuestSnapshots(ctx context.Context) ([]ListAllGuestSnapshotsRow, error) {
	rows, err := q.db.Query(ctx, listAllGuestSnapshots)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAllGuestSnapshotsRow{}
	for rows.Next() {
		var i ListAllGuestSnapshotsRow
		if err := rows.Scan(
			&i.ClusterID,
			&i.Vmid,
			&i.Name,
			&i.GuestType,
			&i.Node,
			&i.Description,
			&i.Parent,
			&i.Vmstate,
			&i.SnapTime,
			&i.LastSeenAt,
			&i.ClusterName,
			&i.VmID,
			&i.VmName,
			&i.VmStatus,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGuestSnapshotsByCluster = `-- name: ListGuestSnapshotsByCluster :many
SELECT cluster_id, vmid, name, guest_type, node, description, parent, vmstate, snap_time, last_seen_at FROM guest_snapshots
WHERE cluster_id = $1
ORDER BY vmid, name
`

func (q *Queries) ListGuestSnapshotsByCluster(ctx context.Context, clusterID uuid.UUID) ([]GuestSnapshot, error) {
	rows, err := q.db.Query(ctx, listGuestSnapshotsByCluster, clusterID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GuestSnapshot{}
	for rows.Next() {
		var i GuestSnapshot
		if err := rows.Scan(
			&i.ClusterID,
			&i.Vmid,
			&i.Name,
			&i.GuestType,
			&i.Node,
			&i.Description,
			&i.Parent,
			&i.Vmstate,
			&i.SnapTime,
			&i.LastSeenAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGuestSnapshotsForReport = `-- name: ListGuestSnapshotsForReport :many
SELECT
    gs.vmid,
    gs.name,
    gs.guest_type,
    gs.node,
    gs.description,
    gs.vmstate,
    gs.snap_time,
    v.name AS vm_name
FROM guest_snapshots gs
LEFT JOIN vms v ON v.cluster_id = gs.cluster_id AND v.vmid = gs.vmid
WHERE gs.cluster_id = $1
ORDER BY (gs.snap_time = 0), gs.snap_time ASC, gs.vmid, gs.name
`

type ListGuestSnapshotsForReportRow struct {
	Vmid        int32       `json:"vmid"`
	Name        string      `json:"name"`
	GuestType   string      `json:"guest_type"`
	Node        string      `json:"node"`
	Description string      `json:"description"`
	Vmstate     bool        `json:"vmstate"`
	SnapTime    int64       `json:"snap_time"`
	VmName      pgtype.Text `json:"vm_name"`
}

// ListGuestSnapshotsForReport feeds the snapshot_inventory report type: one
// cluster's rows, oldest dated first (unknown ages last), with the guest
// name rejoined live (NULL when the guest is gone from inventory).
func (q *Queries) ListGuestSnapshotsForReport(ctx context.Context, clusterID uuid.UUID) ([]ListGuestSnapshotsForReportRow, error) {
	rows, err := q.db.Query(ctx, listGuestSnapshotsForReport, clusterID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListGuestSnapshotsForReportRow{}
	for rows.Next() {
		var i ListGuestSnapshotsForReportRow
		if err := rows.Scan(
			&i.Vmid,
			&i.Name,
			&i.GuestType,
			&i.Node,
			&i.Description,
			&i.Vmstate,
			&i.SnapTime,
			&i.VmName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertGuestSnapshot = `-- name: UpsertGuestSnapshot :one
INSERT INTO guest_snapshots (cluster_id, vmid, name, guest_type, node, description, parent, vmstate, snap_time, last_seen_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9, now())
ON CONFLICT (cluster_id, vmid, name)
DO UPDATE SET
    guest_type = EXCLUDED.guest_type,
    node = EXCLUDED.node,
    description = EXCLUDED.description,
    parent = EXCLUDED.parent,
    vmstate = EXCLUDED.vmstate,
    snap_time = EXCLUDED.snap_time,
    last_seen_at = now()
RETURNING cluster_id, vmid, name, guest_type, node, description, parent, vmstate, snap_time, last_seen_at
`

type UpsertGuestSnapshotParams struct {
	ClusterID   uuid.UUID `json:"cluster_id"`
	Vmid        int32     `json:"vmid"`
	Name        string    `json:"name"`
	GuestType   string    `json:"guest_type"`
	Node        string    `json:"node"`
	Description string    `json:"description"`
	Parent      string    `json:"parent"`
	Vmstate     bool      `json:"vmstate"`
	SnapTime    int64     `json:"snap_time"`
}

func (q *Queries) UpsertGuestSnapshot(ctx context.Context, arg UpsertGuestSnapshotParams) (GuestSnapshot, error) {
	row := q.db.QueryRow(ctx, upsertGuestSnapshot,
		arg.ClusterID,
		arg.Vmid,
		arg.Name,
		arg.GuestType,
		arg.Node,
		arg.Description,
		arg.Parent,
		arg.Vmstate,
		arg.SnapTime,
	)
	var i GuestSnapshot
	err := row.Scan(
		&i.ClusterID,
		&i.Vmid,
		&i.Name,
		&i.GuestType,
		&i.Node,
		&i.Description,
		&i.Parent,
		&i.Vmstate,
		&i.SnapTime,
		&i.LastSeenAt,
	)
	return i, err
}
