Create a new frontend feature module for: $ARGUMENTS

Follow this process:
1. Read the root CLAUDE.md and frontend/src/CLAUDE.md
2. Create the feature directory: frontend/src/features/{name}/
3. Create subdirectories: pages/, components/, hooks/, api/, types/
4. Create the main page component in pages/
5. Create the API client in api/ using TanStack Query patterns
6. Create TypeScript types in types/ matching the OpenAPI schema
7. Add the route to the app router
8. Add the navigation entry to the sidebar
9. Write component tests
10. Run frontend tests and lint
