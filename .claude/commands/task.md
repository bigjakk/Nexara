Execute Nexara development task: $ARGUMENTS

Follow this process:
1. Read CLAUDE.md for project context
2. Read any relevant directory-level CLAUDE.md files
3. Present a plan using "think hard" mode before writing any code
4. Implement in small, testable increments
5. Run tests after each significant change
6. Run `make test` and `make lint` when complete
7. Commit with a conventional commit message
8. If the task involves auth/crypto/RBAC, use the security-reviewer subagent
