node_modules/
.expo/
dist/
web-build/
*.log
.env
.env.local
.env.*.local

# Native
android/
ios/

# Editors
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Build outputs
*.apk
*.aab
*.ipa

# Dev notes — PLAN.md and README.md are personal dev-flow notes
# (phase-by-phase implementation history, local dev setup, UAT
# protocols, "resume from here" checklists, local IPs, host paths,
# etc.) that don't belong in a public repo. Both are kept on the
# developer's machine for reference but excluded from tracking. If
# public-facing mobile docs are needed later, they should be written
# from scratch, not derived from these files.
/README.md
/PLAN.md

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Android signing keys — catastrophic if leaked
*.keystore
*.jks

# iOS provisioning
*.mobileprovision

# Firebase / Google services config
google-services.json
GoogleService-Info.plist

# Expo shared state
.expo-shared/

# Local config overrides
app.config.local.js
app.config.local.ts

# Metro health checks / cache
.metro-health-check*
metro-cache/